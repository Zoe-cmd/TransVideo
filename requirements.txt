# TransVideo 依赖
requests>=2.31.0
openai>=1.12.0
edge-tts>=6.1.9
aiohttp>=3.9.0
numpy>=1.26.0
# YouTube 2026 年起需 JS 运行时解签名（node/deno），旧版 yt-dlp 会 403
yt-dlp>=2026.7.4
# 本地 ASR 引擎（首次使用时自动下载模型，约 1-3GB）
faster-whisper>=1.0.0
# IndexTTS 2.5 模型权重下载（仅 TTS_ENGINE=index 时需要）
modelscope>=1.27.0
